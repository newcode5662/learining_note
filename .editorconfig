# 根配置，表示这是最顶层的配置文件
root = true

# 匹配所有文件
[*]
charset = utf-8
indent_style = space
indent_size = 4
end_of_line = lf
insert_final_newline = true
# 默认移除行尾空格（为了代码整洁）
trim_trailing_whitespace = true

# 【关键点】针对 Markdown 文件进行特殊覆盖
[*.md]
# 允许行尾保留空格（为了支持 "双空格+回车" 的换行语法）
trim_trailing_whitespace = false